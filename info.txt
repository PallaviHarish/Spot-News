rcc - class based component app
rce - for components
Class based:
    c="john"
    ...
    ..
    <div> Hey {this.c} </div>

    //this. is used to access

Constructor-

constructor(){
    super();
}


Warning: Each child in a list should have a unique "key" prop.
    every element in map has a unique key
    thus, key={element.<uniqueFactor>} //in div return element


async componentDidMount() {
    let url =
      "https://newsapi.org/v2/top-headlines?country=in&apiKey=7baa762d5dea47e984171086a38a8198";
    let data = await fetch(url);
    let parsedData = await data.json();
    this.setState({ articles: parsedData.articles });
  }

To use prop types use - impt (to import prop types)